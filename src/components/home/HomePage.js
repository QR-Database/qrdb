import React from "react";

const HomePage = () => (
  <div className="jumbotron">
    <h1>Work in progress app for QRDB</h1>
    <p className="text-size">React, Redux and React Router for ultra-responsive web apps.</p>
  </div>
);

export default HomePage;
